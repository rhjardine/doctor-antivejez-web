{
  "compilerOptions": {
    // Hereda configuraciones base como los alias de 'paths'
    "extends": "./tsconfig.json", 

    // Sobrescribe las opciones clave para un script de Node.js
    "module": "CommonJS",
    "moduleResolution": "node",
    "target": "ES2020",
    "outDir": "dist",
    "rootDir": ".",
    
    // Opciones para asegurar la compilación
    "noEmit": false,
    "isolatedModules": false,
    "skipLibCheck": true
  },
  "include": [
    "prisma/import-legacy.ts" // Especificamos el único archivo que queremos compilar
  ],
  "exclude": [
    "node_modules",
    "dist"
  ]
}